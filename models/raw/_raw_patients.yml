version: 2

models:
  - name: raw_patients
    columns:
      - name: id
        tests: 
        - unique
        - not_null
        # - dbt_utils.equal_rowcount:
        #   compare_model: ref('other_table_name')
              