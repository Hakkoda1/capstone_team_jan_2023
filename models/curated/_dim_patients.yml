version: 2

models:
  - name: dim_patients
    description: This is the curated/final patients model which is now ready for a BI analyst to pull from.
    columns:
      - name: patient_skey
        description: This is the primary key for dim_patients model.
        tests:
          - unique
          - not_null
          